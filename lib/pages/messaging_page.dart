import 'package:flutter/material.dart';

class MessagingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Mesajlaşma"),
      ),
      body: Center(
        child: Text("Mesajlaşma Sayfası"),
      ),
    );
  }
}
